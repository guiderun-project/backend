spring:
  profiles:
    active: local
    group:
      local:
        - jwt
      qa:
        - jwt
      prod:
        - jwt
      batch:
        - jwt
  messages:
    basename: i18n/exception
    encoding: UTF-8
