interface:
  display_name: "PR"
  short_description: "Create project pull requests"
  default_prompt: "Use $pr to create a pull request for the current branch."
policy:
  allow_implicit_invocation: false
